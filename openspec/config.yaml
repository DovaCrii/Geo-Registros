# OpenSpec Configuration — AeroFlow / Geo-Registros
# Auto-generated by sdd-init

project:
  name: AeroFlow
  slug: geo-registros
  description: "Operational hub for RPA/drone flight operations management"
  repo: https://github.com/DovaCrii/Geo-Registros.git

stack:
  frontend: Next.js 15
  language: TypeScript 5.8
  ui: React 19 + Tailwind CSS 3
  backend: Server Actions + Route Handlers
  database: Prisma 6 + SQLite (local)
  auth: NextAuth
  maps: MapLibre GL JS + TerraDraw
  reports: PDFKit + XLSX

testing:
  runner: Vitest 4
  framework: Testing Library + jsdom
  command: npm run test
  typecheck: npm run typecheck
  strict_tdd: true
  layers:
    - unit
    - component

lint: none
format: none

persistence:
  mode: openspec
  path: openspec/
